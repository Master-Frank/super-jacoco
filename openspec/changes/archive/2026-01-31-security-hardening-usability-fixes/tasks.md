## 1. API 访问控制

- [x] 1.1 增加接口鉴权配置项与 Token 校验组件
- [x] 1.2 为 `/cov/**` 接入统一拦截器或过滤器
- [x] 1.3 增加可配置 IP allowlist 并接入拦截链路
- [x] 1.4 为高成本触发接口增加并发保护与明确错误返回

## 2. 安全命令执行

- [x] 2.1 新增参数化外部命令执行器（超时、退出码、输出采集）
- [x] 2.2 替换现有 `bash -c` 命令拼接链路为参数化执行
- [x] 2.3 增加输入校验与白名单规则（uuid、gitUrl、version、subModule、路径）
- [x] 2.4 统一文件清理/拷贝为 Java NIO 实现并限制在工作根目录内

## 3. 可用性修复

- [x] 3.1 修复 Maven profile 参数拼接与 Readme 示例对齐
- [x] 3.2 修复单/多模块判断逻辑并确保生成报告参数正确
- [x] 3.3 路径与产物位置可配置化（工作目录、报告目录、JaCoCo CLI/资源）

## 4. 任务可靠性与可观测性

- [x] 4.1 将 `new Thread()` 改为统一线程池执行并加并发上限
- [x] 4.2 改进异常处理与日志输出，确保打印完整堆栈且返回安全错误信息
- [x] 4.3 统一任务状态流转与失败恢复策略，减少重复执行与状态不一致

## 5. 验证

- [x] 5.1 完成构建与基础启动验证，确保核心接口链路可用
- [x] 5.2 检查关键场景：未授权拦截、非法输入拒绝、超时/退出码处理
